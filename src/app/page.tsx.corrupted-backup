'use client';

import { useState, useEffect } from 'react';
import { motion } from 'framer-motion';

export default function SovereignDashboard() {
  const [activeWorker, setActiveWorker] = useState('brain');
  const [metrics, setMetrics] = useState({
    cpu: 18,
    ram: 13,
    gpuTemp: 46,
    gpuUsage: 18,
    networkDown: 4.36,
    networkUp: 2.14,
    activeConnections: 33
  });

  const workers = [
    { id: 'brain', name: 'Brain', icon: '🧠', model: 'Llama 3.2 3B', active: true },
    { id: 'eyes', name: 'Eyes', icon: '👁️', model: 'Llava 7B', active: false },
    { id: 'hands', name: 'Hands', icon: '✋', model: 'Qwen 2.5', active: false },
    { id: 'memory', name: 'Memory', icon: '📁', model: 'Context', active: false }
  ];

  return (
    <div className="min-h-screen bg-gradient-to-br from-[#030405] via-[#0a0e14] to-[#030405] text-white">
      {/* Background Effects */}
      <div className="fixed inset-0 pointer-events-none">
        <div className="absolute top-[-20%] left-[-10%] w-[50%] h-[50%] bg-cyan-500/5 blur-[150px] rounded-full" />
        <div className="absolute bottom-[-20%] right-[-10%] w-[50%] h-[50%] bg-purple-500/5 blur-[150px] rounded-full" />
      </div>

      {/* Main Container */}
      <div className="relative max-w-7xl mx-auto p-8 pt-16">
        {/* Header */}
        <motion.div 
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          className="text-center mb-16"
        >
          <h1 className="text-7xl font-bold bg-gradient-to-r from-cyan-400 via-purple-400 to-pink-400 bg-clip-text text-transparent mb-4 tracking-tight">
            REZ HIVE
          </h1>
          <div className="flex items-center justify-center gap-4 text-sm">
            <div className="flex items-center gap-2 px-4 py-2 bg-white/5 rounded-full border border-white/10">
              <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse" />
              <span className="text-white/60">Sovereign Mode</span>
            </div>
            <div className="flex items-center gap-2 px-4 py-2 bg-white/5 rounded-full border border-white/10">
              <span className="text-cyan-400">⚡</span>
              <span className="text-white/60">Zero-Drift Active</span>
            </div>
          </div>
        </motion.div>

        {/* Workers Grid */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.1 }}
          className="grid grid-cols-4 gap-4 mb-12"
        >
          {workers.map((worker) => (
            <button
              key={worker.id}
              onClick={() => setActiveWorker(worker.id)}
              className={`relative p-6 rounded-2xl transition-all duration-300 ${
                activeWorker === worker.id
                  ? 'bg-gradient-to-br from-cyan-500/20 to-purple-500/20 border-2 border-cyan-500/50 shadow-[0_0_30px_rgba(6,182,212,0.3)]'
                  : 'bg-white/5 border border-white/10 hover:bg-white/10'
              }`}
            >
              <div className="text-4xl mb-3">{worker.icon}</div>
              <div className="text-xl font-bold mb-1">{worker.name}</div>
              <div className="text-sm text-white/40 mb-3">{worker.model}</div>
              {worker.active && (
                <div className="absolute top-4 right-4 flex items-center gap-1.5">
                  <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse" />
                  <span className="text-xs text-green-400">ACTIVE</span>
                </div>
              )}
              {activeWorker === worker.id && (
                <motion.div
                  layoutId="active-worker"
                  className="absolute bottom-0 left-1/2 -translate-x-1/2 w-12 h-1 bg-cyan-400 rounded-full"
                />
              )}
            </button>
          ))}
        </motion.div>

        {/* Metrics Grid */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.2 }}
          className="grid grid-cols-4 gap-6 mb-12"
        >
          {/* CPU Card */}
          <div className="bg-white/5 rounded-2xl p-6 border border-white/10 backdrop-blur-sm">
            <div className="flex items-center justify-between mb-4">
              <span className="text-sm font-medium text-white/40">CPU</span>
              <span className="text-2xl font-bold text-cyan-400">{metrics.cpu}%</span>
            </div>
            <div className="h-2 bg-white/10 rounded-full overflow-hidden">
              <motion.div 
                className="h-full bg-gradient-to-r from-cyan-500 to-blue-500"
                initial={{ width: 0 }}
                animate={{ width: `${metrics.cpu}%` }}
                transition={{ duration: 1 }}
              />
            </div>
          </div>

          {/* RAM Card */}
          <div className="bg-white/5 rounded-2xl p-6 border border-white/10 backdrop-blur-sm">
            <div className="flex items-center justify-between mb-4">
              <span className="text-sm font-medium text-white/40">RAM</span>
              <span className="text-2xl font-bold text-purple-400">{metrics.ram}%</span>
            </div>
            <div className="h-2 bg-white/10 rounded-full overflow-hidden">
              <motion.div 
                className="h-full bg-gradient-to-r from-purple-500 to-pink-500"
                initial={{ width: 0 }}
                animate={{ width: `${metrics.ram}%` }}
                transition={{ duration: 1 }}
              />
            </div>
          </div>

          {/* GPU Card */}
          <div className="bg-white/5 rounded-2xl p-6 border border-white/10 backdrop-blur-sm col-span-2">
            <div className="flex items-center justify-between mb-4">
              <span className="text-sm font-medium text-white/40">GPU (RTX 3060)</span>
              <div className="flex items-center gap-4">
                <span className="text-orange-400">{metrics.gpuTemp}°C</span>
                <span className="text-2xl font-bold text-orange-400">{metrics.gpuUsage}%</span>
              </div>
            </div>
            <div className="h-2 bg-white/10 rounded-full overflow-hidden">
              <motion.div 
                className="h-full bg-gradient-to-r from-orange-500 to-red-500"
                initial={{ width: 0 }}
                animate={{ width: `${metrics.gpuUsage}%` }}
                transition={{ duration: 1 }}
              />
            </div>
          </div>
        </motion.div>

        {/* Network & Memory Row */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.3 }}
          className="grid grid-cols-3 gap-6 mb-12"
        >
          {/* Network Card */}
          <div className="bg-white/5 rounded-2xl p-6 border border-white/10 backdrop-blur-sm col-span-2">
            <div className="flex items-center justify-between mb-6">
              <span className="text-sm font-medium text-white/40">NETWORK</span>
              <span className="text-emerald-400">{metrics.activeConnections} ACTIVE</span>
            </div>
            
            <div className="space-y-4">
              {/* Download */}
              <div>
                <div className="flex justify-between text-xs mb-1">
                  <span className="text-white/40">DOWNLOAD</span>
                  <span className="text-white/80">{metrics.networkDown} MB/s</span>
                </div>
                <div className="h-1.5 bg-white/10 rounded-full overflow-hidden">
                  <motion.div 
                    className="h-full bg-emerald-400"
                    initial={{ width: 0 }}
                    animate={{ width: `${Math.min(metrics.networkDown * 10, 100)}%` }}
                    transition={{ duration: 1 }}
                  />
                </div>
              </div>
              
              {/* Upload */}
              <div>
                <div className="flex justify-between text-xs mb-1">
                  <span className="text-white/40">UPLOAD</span>
                  <span className="text-white/80">{metrics.networkUp} MB/s</span>
                </div>
                <div className="h-1.5 bg-white/10 rounded-full overflow-hidden">
                  <motion.div 
                    className="h-full bg-cyan-400"
                    initial={{ width: 0 }}
                    animate={{ width: `${Math.min(metrics.networkUp * 20, 100)}%` }}
                    transition={{ duration: 1 }}
                  />
                </div>
              </div>
            </div>
          </div>

          {/* Memory Card */}
          <div className="bg-white/5 rounded-2xl p-6 border border-white/10 backdrop-blur-sm">
            <div className="flex items-center justify-between mb-4">
              <span className="text-sm font-medium text-white/40">MEMORY CONTEXT</span>
              <button className="text-xs text-cyan-400 hover:text-cyan-300 transition-colors">
                VIEW ALL →
              </button>
            </div>
            <div className="space-y-3">
              <div className="p-3 bg-white/5 rounded-lg border border-white/5">
                <div className="text-xs text-cyan-400 mb-1">Session active</div>
                <div className="text-sm text-white/80">5 active contexts</div>
              </div>
              <div className="p-3 bg-white/5 rounded-lg border border-white/5">
                <div className="text-xs text-purple-400 mb-1">Last query</div>
                <div className="text-sm text-white/80">2 min ago</div>
              </div>
            </div>
          </div>
        </motion.div>

        {/* Brain Worker Status */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.4 }}
          className="bg-gradient-to-r from-cyan-500/10 to-purple-500/10 rounded-2xl p-8 border border-cyan-500/20 mb-8 text-center"
        >
          <div className="text-6xl mb-4">🧠</div>
          <h2 className="text-2xl font-bold mb-2">Brain Worker</h2>
          <p className="text-white/40">(Llama 3.2 3B) is awaiting cognitive tasks.</p>
        </motion.div>

        {/* Query Input */}
        <motion.div 
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.5 }}
          className="relative mb-8"
        >
          <input
            type="text"
            placeholder="🏛️ Message Brain Worker..."
            className="w-full bg-white/5 border border-white/10 rounded-2xl px-6 py-4 text-lg focus:outline-none focus:border-cyan-500/50 focus:bg-white/10 transition-all placeholder:text-white/20"
          />
          <button className="absolute right-3 top-1/2 -translate-y-1/2 px-6 py-2 bg-gradient-to-r from-cyan-500 to-purple-500 rounded-xl text-white font-medium hover:opacity-90 transition-opacity">
            Send
          </button>
        </motion.div>

        {/* Governor Footer */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.6 }}
          className="flex items-center justify-center gap-3 text-sm"
        >
          <div className="flex items-center gap-2 px-4 py-2 bg-white/5 rounded-full border border-white/10">
            <span className="text-cyan-400">⚖️</span>
            <span className="text-white/40">GOVERNOR</span>
            <span className="text-cyan-400 font-bold">CONSTITUTIONAL</span>
          </div>
          <div className="flex items-center gap-2 px-4 py-2 bg-white/5 rounded-full border border-white/10">
            <span className="text-green-400">🛡️</span>
            <span className="text-white/40">Article</span>
            <span className="text-green-400 font-bold">9 ACTIVE</span>
          </div>
        </motion.div>

        {/* PS1 Controller will appear here automatically */}
      </div>
    </div>
  );
}